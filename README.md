# Fundonor 基金助手

<p align="center">
  <img src="src/assets/icon-outline.png" alt="Logo" width="128" />
</p>

一款简洁、高效的桌面端实时基金估值与持仓监控工具。基于 Electron 与 React 构建，数据本地存储，安全无忧。

## ✨ 主要功能

*   **实时估值**：对接实时数据源，盘中实时更新基金估值涨跌幅。
*   **持仓管理**：
    *   支持创建多个持仓组合（Portfolio）。
    *   自动计算当日盈亏、持有收益、持有收益率。
    *   支持记录买入成本，精准核算投资回报。
*   **自选关注**：
    *   便捷搜索添加基金。
    *   分组管理自选列表。
    *   分时走势图与历史净值曲线（近10日）。
*   **贴心细节**：
    *   **多语言支持**：一键切换 中文 / English 界面。
    *   **隐私安全**：使用 SQLite 本地存储所有数据，主要数据不上传服务器。
    *   **跨平台体验**：
        *   Windows：支持关闭时最小化到系统托盘，后台静默运行。
        *   macOS：适配原生窗口风格与操作习惯。

## 🚀 快速开始

### 环境依赖

*   Node.js (推荐 v18+)
*   npm 或 yarn

### 1. 安装依赖

```bash
npm install
```

> **注意**：本项目使用了 `better-sqlite3`，在某些系统环境下安装可能需要构建工具（Windows 需要 Visual Studio Build Tools，macOS 需要 Xcode Command Line Tools）。如果遇到原生模块报错，请尝试运行 `npm run rebuild`。

### 2. 启动开发环境

```bash
npm run electron:dev
```
该命令将同时启动 Vite 前端服务（热更新）与 Electron 主进程。

### 3. 打包应用

构建适用于当前操作系统的安装包（exe / dmg）：

```bash
npm run electron:build
```
构建产出文件将位于 `release/` 目录下。

## 🛠️ 技术栈

*   **Electron**: 跨平台桌面应用外壳
*   **React 19**: 用户界面构建
*   **Vite**: 高效的前端构建工具
*   **Tailwind CSS**: 实用主义 CSS 框架
*   **better-sqlite3**: 高性能本地数据库
*   **Lucide React**: 精美图标库

## 📄 许可证

MIT License
